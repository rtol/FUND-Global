vtemp = observedImpact(:,1);
vimp = observedImpact(:,2);
vN = length(vtemp);
vtemp = vtemp(1:vN-1);
vimp = vimp(1:vN-1);

imppar = zeros(NImpact,3);

[imppar(1,1) impar(1,2) impar (1,3)] = fittol(temp,imp);
[imppar(2,1) impar(2,2) impar (1,3)] = fitweitzman(temp,imp);
[imppar(3,1) impar (3,3)] = fittol(temp,imp);
[imppar(4,1) impar (5,3)] = fittol(temp,imp);
[imppar(5,1) impar (5,3)] = fittol(temp,imp);